name: Mi citio Web Kevin Liriano

on:
  push:
    branches:
      - master

jobs:
  alerta:
    runs-on: ubuntu-latest

    steps:
      - name: Enviar alerta
        run: |
          curl -d "Nuevo push realizado en main" https://ntfy.sh/devops-itla